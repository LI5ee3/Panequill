# AGENTS.md

本文件定义 Codex 在 Panequill 仓库中的全局执行规则。

Codex 仅作为执行者，根据现有权威文档、ROADMAP 和明确任务实现代码，不自行重新定义产品、架构、数据、安全、设计或测试规则。

## 权威文档

- `docs/PRODUCT.md`：产品范围与产品目标的权威来源。
- `docs/ARCHITECTURE.md`：技术架构与实现边界的权威来源。

Codex 只能读取、理解并执行这些权威文档中的要求，不得自行修改这些文档。

## 执行规则

1. 仅实现任务明确要求的内容，不自行扩展需求。
2. 不为假设中的未来需求增加抽象层、兼容层、备用方案或额外服务。
3. 优先复用项目现有结构、依赖和接口，不自行创造重复实现。
4. 修改前先理解相关代码与约束，避免破坏既有架构。
5. 任务描述与权威文档冲突时，停止实现并指出冲突，不自行决定取舍。
6. 遇到无法从代码、文档或任务中确认的信息时，不猜测业务规则。
7. 完成修改后必须检查所有改动是否符合任务要求和权威文档，并执行当前项目已有的相关测试、检查或构建命令。
8. 不为了通过测试而删除、绕过或弱化已有校验。
9. 不修改与当前任务无关的文件。
10. 保持实现简单直接；能用现有方案解决的问题，不进行无必要的重构。

## 文档修改

除非任务明确要求修改文档，否则不得修改 `docs/` 下的权威文档。

如果实现过程中发现权威文档需要变更，应停止相关实现并报告原因，由用户先确认文档变更。
